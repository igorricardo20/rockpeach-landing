export interface Project {
  id: number;
  title: string;
  description: string;
  image: string;
  type: string;
}

export interface WorkProcess {
  id: number;
  title: string;
  description: string;
  icon: string;
}